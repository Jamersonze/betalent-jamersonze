import React from 'react';
import '../styles/Header.css';

const Header: React.FC = () => {
    return (
        <header>
            <img src="/BeTalent.svg" alt="Logo do BeTalent" />
        </header>
    );
};

export default Header;